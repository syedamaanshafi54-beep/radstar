
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    function isAdmin() {
      // In a real app, this would check a custom claim: request.auth.token.admin == true;
      // Using email for demonstration purposes.
      return request.auth != null && request.auth.token.email == 'itsmeabdulk@gmail.com';
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }
    
    // PUBLIC COLLECTIONS
    match /products/{productId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    match /site-config/{configId} {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    // PARTNER SUBMISSIONS
    match /partners/{partnerId} {
      allow create: if true;
      allow read, update, delete: if isAdmin();
    }

    // USER DATA ACCESS RULES (for single document reads/writes)
    match /users/{userId} {
      // Admins can read/write any user's data for management.
      // Users can only read/write their own data.
      allow get, write: if isOwner(userId) || isAdmin();
      
      // Only admins can delete user documents.
      allow delete: if isAdmin();

      // Subcollections
      match /customers/{customerId} {
        allow read, write: if isOwner(userId) || isAdmin();
      }
      
      match /orders/{orderId} {
        allow read, write: if isOwner(userId) || isAdmin();
          
        match /orderItems/{orderItemId} {
          allow read, write: if isOwner(userId) || isAdmin();
        }
      }

      match /addresses/{addressId} {
        allow read, write: if isOwner(userId) || isAdmin();
      }
    }
     
    // --- ADMIN LISTING RULES ---
    // Admins can list all user documents for the admin customer page.
    match /users/{userId} {
      allow list: if isAdmin();
    }
    
    // Allow admins to list all orders across all users for the admin panel.
    match /{path=**}/orders/{orderId} {
      allow list: if isAdmin();
    }
  }
}
